# Linux build-only environment; the application uses Python's standard library.
# Versions verified against the installed, pinned project build toolchain.
PyInstaller==6.22.3
altgraph==0.17.5
packaging==26.3
pyinstaller-hooks-contrib==2026.7
setuptools==84.0.0
